<div class="random-beers-all">
    <div>
    <img id="beer-image" src="{{this.image_url}}" alt="{{this.name}}">
    </div>
    <div class="random-beers">
    <h4>{{this.name}}</h4>
    <p><span>Description:</span> {{this.description}}</p>
    <p><span>Tagline:</span> {{this.tagline}}</p>
    </div>
</div>
